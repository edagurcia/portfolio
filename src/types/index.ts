export type SVGType = {
  width: number;
  height: number;
  className?: string;
};

export type Heroe = {
  title: string;
  subtitle: string;
  slogan: string;
};

export type ToolBox = {
  title: string;
  icon: string;
  width: number;
  height: number;
  className?: string;
};
